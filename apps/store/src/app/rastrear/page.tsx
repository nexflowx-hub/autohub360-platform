import { redirect } from 'next/navigation';
import { PackageSearch } from 'lucide-react';
import { Container } from '@autohub360/ui';

export const metadata={title:'Rastrear pedido',description:'Acompanhe as etapas de entrega do seu pedido AutoHub360.',robots:{index:false,follow:false}};
export default async function TrackingSearch({searchParams}:{searchParams:Promise<{code?:string}>}){const q=await searchParams;const code=q.code?.trim();if(code)redirect(`/rastrear/${encodeURIComponent(code.toUpperCase())}`);return <Container className="py-12 sm:py-16"><div className="mx-auto max-w-xl rounded-2xl border border-surface-200 bg-white p-7 text-center shadow-[var(--ah-shadow-card)]"><span className="mx-auto flex h-14 w-14 items-center justify-center rounded-full bg-ahblue-500/10 text-ahblue-600"><PackageSearch className="h-7 w-7"/></span><h1 className="mt-5 font-display text-2xl font-extrabold text-ink-900">Rastrear entrega</h1><p className="mt-2 text-sm text-ink-500">Use o código público AutoHub360 (AH…) recebido nas atualizações do pedido.</p><form className="mt-6 flex gap-2"><input name="code" required placeholder="Ex.: AH1A2B3C4D5E6F" className="h-11 min-w-0 flex-1 rounded-lg border border-surface-300 px-3 text-sm outline-none focus:border-ahblue-500"/><button className="h-11 rounded-lg bg-ahblue-500 px-5 font-display text-sm font-bold text-white hover:bg-ahblue-600">Consultar</button></form></div></Container>}
